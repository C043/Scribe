services:
  scribe:
    build: .
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    image: scribe
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
